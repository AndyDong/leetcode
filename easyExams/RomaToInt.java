package easyExams;

import java.util.Scanner;

public class RomaToInt {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        while (sc.hasNext()) {
            System.out.println(romanToInt(sc.nextInt()));

        }
    }
    private static int romanToInt(int x) {
        return 0;
    }
}
